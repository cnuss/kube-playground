#!/usr/bin/env bash
set -Eeuo pipefail

# TODO prebuild persistence
# mkdir -p /workspaces/.minikube
# minikube status || minikube start --driver=docker --mount --mount-string="/workspaces/.minikube:/var/lib/minikube"

minikube config set cpus 2
minikube config set memory 8096

minikube start
minikube addons enable ingress
minikube addons enable ingress-dns
