releases:
  - name: bootstrap
    namespace: default
    chart: ./bootstrap
    createNamespace: false
    values:
      - github:
          serverUrl: '{{ requiredEnv "GITHUB_SERVER_URL" }}'
          repository: '{{ requiredEnv "GITHUB_REPOSITORY" }}'
          user: '{{ env "GITHUB_USER" | default "x-access-token" }}'
          token: '{{ requiredEnv "GITHUB_TOKEN" }}'
 